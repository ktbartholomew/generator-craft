/*global require*/
(function () {
  'use strict';
}());

require.config({
  paths: {
    // Set custom paths to things to make them easier to include
  },
}); 

require([
	'modules/analytics',
  'modules/nav',
  'modules/hero',
], function () {
  
  'use strict';
  // You could define some functions to run here, or you could just include
  // modules above.

});
